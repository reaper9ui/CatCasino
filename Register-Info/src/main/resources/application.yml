spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/registerinfo
    username: root
    password: theg@ngstA1
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect # Use appropriate MySQL dialect
    hibernate:
      ddl-auto: update # Configure DDL behavior (e.g., update, create, validate)
